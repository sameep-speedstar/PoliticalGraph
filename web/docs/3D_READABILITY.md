# 3D readability notes

## Shipped (this pass)
- Default camera farther out; zoom-out range up to ~32
- Very light translucent **octant boxes** (8 regions)
- Outer wire cube for ±100 bounds
- Vertical **depth stems** + floor footprints (Authority height readable)
- Name labels on nodes (all when ≤20 / highlighted set)
- Orbit hint overlay

## Further options (if still hard to read)
1. **Preset views** — buttons: Economic×Authority, Economic×Cultural, Authority×Cultural (orthographic 2D slices)
2. **Label density slider** — always / nearest-N / hover-only
3. **Focus mode** — dim all but selected + nearest 5; draw connection lines
4. **Search highlight pulse** on explore
5. **Side radar / bar chart** synced to selection (2D backup of the same coords)
6. **Color nodes by cluster** instead of uniform ink
7. **Reduce occlusion** — slightly larger spacing scale or fish-eye on hover
