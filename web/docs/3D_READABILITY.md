# 3D readability notes

## Shipped (this pass)
- Default camera farther out; zoom-out range up to ~32
- Very light translucent **octant boxes** (8 regions)
- Outer wire cube for ±100 bounds
- Vertical **depth stems** + floor footprints (Authority height readable)
- Name labels on nodes (all when ≤24 / highlighted set)
- **View presets:** 3D · E×A · E×C · A×C
- Orbit hint overlay

## Further options (if still hard to read)
1. Label density slider — always / nearest-N / hover-only
2. Focus mode — dim all but selected + nearest 5; draw connection lines
3. Search highlight pulse on explore
4. Side radar / bar chart synced to selection
5. Color nodes by thinking-group cluster
6. Slightly larger spacing scale if crowding grows past ~50 figures
